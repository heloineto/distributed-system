import MainShell from '@components/app-shells/MainShell';
import UpdateComponent from '@components/general/auth/Update';

const Update: NextPage = () => {
  return (
    <MainShell>
      <UpdateComponent />
    </MainShell>
  );
};

export default Update;
